version: "2"

services:
  wildfly:
    image: jboss/wildfly
    ports:
      - "8080:8080"
    environment:
      - app=${APP}
    networks:
      - wfymonet

  db:
    image: mongo
    ports:
      - "27017:27017"
    networks:
      - wfymonet

networks:
  wfymonet:
    driver: bridge
